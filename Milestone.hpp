#ifndef MILESTONE_HPP
#define MILESTONE_HPP
#include <iostream>

class Milestone
{
    private:
        std::string title;
        std::string date_end;
        std::string date_start;
    public:
        Milestone(/* args */);
        ~Milestone();
};


#endif